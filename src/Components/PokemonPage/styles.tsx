import {StyleSheet} from "react-native";

const styles = StyleSheet.create({
    container: {
        flex: 1,
        alignItems: "center",
    },
    pokeName: {
        fontSize: 32,
    }
});
export default styles;
